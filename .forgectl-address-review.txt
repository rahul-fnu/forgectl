A code review has requested changes on this branch. Address ALL of the following review comments:

1. [MUST_FIX] test/unit/orchestrator-scheduler.test.ts:355 — Debug `console.log` statements are left in the test (`console.log("after first flush: ...")`  etc.). These pollute CI output and were clearly only added during development. Four occurrences in this test case.
   Suggested fix: Remove all four `console.log` lines from the `schedules next tick after poll_interval_ms` test.

2. [SHOULD_FIX] src/orchestration/scheduler.ts:397 — When `stop()` is called while the loop is awaiting the `setTimeout` promise (lines 397-402), `clearTimeout` prevents the callback from firing, so `resolve()` is never called. The `await` hangs forever, leaving a dangling async frame. In a long-running daemon this is a minor resource leak, and it makes it impossible to cleanly await shutdown.
   Suggested fix: Store the `resolve` reference externally (e.g. `let pendingResolve`) and call it in the stop function after `clearTimeout`, so the promise settles and the loop exits cleanly.

3. [NIT] src/orchestrator/scheduler.ts:375 — The `delay()` helper is defined but never used — the loop uses an inline `new Promise(resolve => setTimeout(...))` instead.
   Suggested fix: Either use `await delay(deps.config.orchestrator.poll_interval_ms)` inside the loop (and track `pendingTimer` differently), or remove the dead `delay` function.

Rules:
- Read each flagged file before making changes
- Address EVERY comment listed above — do not skip any
- Follow the suggested fixes when provided
- Make minimal changes — only fix what the review flagged
- Run the build and tests after making changes to verify nothing breaks
- Stage all changes with git add