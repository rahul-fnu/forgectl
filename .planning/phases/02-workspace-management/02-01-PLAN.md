---
phase: 02-workspace-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workspace/safety.ts
  - src/workspace/hooks.ts
  - src/config/schema.ts
  - test/unit/workspace-safety.test.ts
  - test/unit/workspace-hooks.test.ts
autonomous: true
requirements:
  - R3.2
  - R3.3

must_haves:
  truths:
    - "Identifiers with special characters are sanitized to [A-Za-z0-9._-]"
    - "Path traversal attempts throw descriptive errors"
    - "Dot-only and empty identifiers are rejected"
    - "Hooks execute shell commands with workspace as cwd"
    - "Hook timeout kills the process and throws"
    - "Hook failure includes hook name, exit code, and stderr in error message"
  artifacts:
    - path: "src/workspace/safety.ts"
      provides: "sanitizeIdentifier and assertContainment functions"
      exports: ["sanitizeIdentifier", "assertContainment"]
    - path: "src/workspace/hooks.ts"
      provides: "executeHook function with timeout support"
      exports: ["executeHook"]
    - path: "src/config/schema.ts"
      provides: "WorkspaceConfigSchema added to config"
      contains: "WorkspaceConfigSchema"
    - path: "test/unit/workspace-safety.test.ts"
      provides: "Safety function tests"
    - path: "test/unit/workspace-hooks.test.ts"
      provides: "Hook execution tests"
  key_links:
    - from: "src/workspace/hooks.ts"
      to: "node:child_process"
      via: "execFile with timeout option"
      pattern: "execFile.*timeout"
    - from: "src/workspace/safety.ts"
      to: "node:path"
      via: "resolve + startsWith for containment"
      pattern: "resolve.*startsWith"
---

<objective>
Create the safety utilities, hook executor, and config schema for workspace management.

Purpose: Establish the foundational modules that WorkspaceManager (Plan 02) will consume -- path safety, hook execution, and config validation.
Output: `src/workspace/safety.ts`, `src/workspace/hooks.ts`, config schema extension, and tests for both.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workspace-management/02-CONTEXT.md
@.planning/phases/02-workspace-management/02-RESEARCH.md
@src/config/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safety module and config schema</name>
  <files>src/workspace/safety.ts, src/config/schema.ts, test/unit/workspace-safety.test.ts</files>
  <behavior>
    - sanitizeIdentifier("issue#123") returns "issue_123"
    - sanitizeIdentifier("normal-id.1") returns "normal-id.1" (unchanged)
    - sanitizeIdentifier("///") throws (sanitizes to empty-equivalent)
    - sanitizeIdentifier("..") throws (unsafe dot-only)
    - sanitizeIdentifier(".") throws (unsafe dot-only)
    - assertContainment("/root", "/root/child") passes (no throw)
    - assertContainment("/root", "/root/../escape") throws with "Path traversal detected"
    - assertContainment("/root", "/other") throws with "Path traversal detected"
    - assertContainment("/root", "/root") passes (root itself is valid)
    - WorkspaceConfigSchema validates with defaults (root: "~/.forgectl/workspaces", hooks: {}, hook_timeout: "60s")
    - WorkspaceConfigSchema validates custom hooks config
  </behavior>
  <action>
    Create `src/workspace/safety.ts` with two exported functions:

    1. `sanitizeIdentifier(identifier: string): string` -- Replace non-`[A-Za-z0-9._-]` chars with `_`. Throw if result is empty, ".", or "..". Per user decision: descriptive error messages.

    2. `assertContainment(root: string, target: string): void` -- Use `path.resolve()` on both, check that resolved target starts with resolved root + path.sep OR equals resolved root. Throw with "Path traversal detected: ..." message on failure.

    Add `WorkspaceConfigSchema` to `src/config/schema.ts`:
    - `root`: string, default `"~/.forgectl/workspaces"`
    - `hooks`: object with optional `after_create`, `before_run`, `after_run`, `before_remove` string fields, default `{}`
    - `hook_timeout`: use existing duration schema, default `"60s"`
    - Add `workspace: WorkspaceConfigSchema` to the main config schema (optional field)
    - Export `WorkspaceConfigSchema` and its inferred type

    Write tests first in `test/unit/workspace-safety.test.ts` using vitest. Test all behaviors listed above.
  </action>
  <verify>
    <automated>npx vitest run test/unit/workspace-safety.test.ts</automated>
  </verify>
  <done>sanitizeIdentifier and assertContainment pass all unit tests. WorkspaceConfigSchema added to config schema and validates correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook executor</name>
  <files>src/workspace/hooks.ts, test/unit/workspace-hooks.test.ts</files>
  <behavior>
    - executeHook runs command with cwd set to workspace path
    - executeHook with successful command resolves without error
    - executeHook with failing command throws with hook name, exit code, and stderr in message
    - executeHook with timeout throws with "timed out" message
    - executeHook error message truncates stderr to 500 chars
  </behavior>
  <action>
    Create `src/workspace/hooks.ts` with one exported function:

    `executeHook(hookName: string, command: string, cwd: string, timeoutMs: number): Promise<void>`

    Implementation: Use `promisify(execFile)` from `node:child_process`. Call `execFile("/bin/sh", ["-c", command], { cwd, timeout: timeoutMs, maxBuffer: 1024 * 1024 })`. On error:
    - If `killed` is true: throw `Hook "${hookName}" timed out after ${timeoutMs}ms`
    - Otherwise: throw `Hook "${hookName}" failed (exit ${code}): ${stderr.trim().slice(0, 500)}`

    Write tests first in `test/unit/workspace-hooks.test.ts`. Use vitest with `vi.mock("node:child_process")` to mock `execFile`. Test: successful execution, failure with exit code and stderr, timeout (killed=true), stderr truncation. Do NOT run actual shell commands in tests.
  </action>
  <verify>
    <automated>npx vitest run test/unit/workspace-hooks.test.ts</automated>
  </verify>
  <done>executeHook passes all unit tests covering success, failure with error details, timeout, and stderr truncation.</done>
</task>

</tasks>

<verification>
npx vitest run test/unit/workspace-safety.test.ts test/unit/workspace-hooks.test.ts
</verification>

<success_criteria>
- safety.ts exports sanitizeIdentifier and assertContainment with correct behavior
- hooks.ts exports executeHook with timeout and error reporting
- Config schema extended with workspace section
- All unit tests pass
- No regressions: FORGECTL_SKIP_DOCKER=true npm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-workspace-management/02-01-SUMMARY.md`
</output>
