---
phase: 24-self-correction-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/runner.ts
  - src/pipeline/coverage.ts
  - test/unit/pipeline-self-correction.test.ts
autonomous: true
requirements:
  - CORR-01
  - CORR-03
  - CORR-04
  - CORR-05

must_haves:
  truths:
    - "ValidationResult exposes lastOutput with combined stdout+stderr from final validation pass"
    - "extractCoverage parses vitest, jest/istanbul, and c8 coverage formats from test output"
    - "extractCoverage returns -1 when no coverage pattern is found"
    - "Test scaffold exists with failing tests for no-progress detection, exclusion enforcement, coverage parsing"
  artifacts:
    - path: "src/validation/runner.ts"
      provides: "ValidationResult.lastOutput field populated from validation step results"
      contains: "lastOutput"
    - path: "src/pipeline/coverage.ts"
      provides: "extractCoverage utility function"
      exports: ["extractCoverage"]
    - path: "test/unit/pipeline-self-correction.test.ts"
      provides: "Test scaffold for CORR-01, CORR-02, CORR-04, CORR-05"
      min_lines: 50
  key_links:
    - from: "src/validation/runner.ts"
      to: "src/orchestration/single.ts"
      via: "ValidationResult type flows through ExecutionResult.validation"
      pattern: "validation:\\s*ValidationResult"
---

<objective>
Surface validation output for downstream consumption and create the coverage extraction utility and test scaffold.

Purpose: Plan 02 needs ValidationResult.lastOutput and extractCoverage to wire no-progress detection, exclusion enforcement, and coverage variable injection into executeLoopNode. This plan creates those foundations plus the test file that Plan 02's tests will extend.

Output: Modified runner.ts with lastOutput, new coverage.ts utility, new test file with scaffolded tests.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-self-correction-integration/24-CONTEXT.md
@.planning/phases/24-self-correction-integration/24-RESEARCH.md

@src/validation/runner.ts
@src/validation/step.ts
@src/orchestration/single.ts
@src/pipeline/executor.ts
@test/unit/pipeline-loop.test.ts

<interfaces>
<!-- Key types the executor needs -->

From src/validation/runner.ts:
```typescript
export interface ValidationResult {
  passed: boolean;
  totalAttempts: number;
  stepResults: Array<{
    name: string;
    passed: boolean;
    attempts: number;
  }>;
}
```

From src/validation/step.ts:
```typescript
export interface StepResult {
  step: ValidationStep;
  passed: boolean;
  exitCode: number;
  stdout: string;
  stderr: string;
  durationMs: number;
}
```

From src/orchestration/single.ts:
```typescript
export interface ExecutionResult {
  success: boolean;
  output?: OutputResult;
  validation: ValidationResult;
  durationMs: number;
  error?: string;
  review?: ReviewSummary;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lastOutput to ValidationResult and create extractCoverage utility</name>
  <files>src/validation/runner.ts, src/pipeline/coverage.ts</files>
  <behavior>
    - ValidationResult.lastOutput contains concatenated stdout+stderr from all steps in the final validation pass
    - ValidationResult.lastOutput is undefined when no validation steps are configured (empty steps array)
    - extractCoverage("All files | 72.34 | ...") returns 72.34 (vitest format)
    - extractCoverage("Statements   : 85.5% ( 100/117 )") returns 85.5 (jest/istanbul format)
    - extractCoverage("Lines        : 91.2% ( 200/219 )") returns 91.2 (c8/istanbul Lines format)
    - extractCoverage("no coverage info here") returns -1 (sentinel for not found)
    - extractCoverage("") returns -1
  </behavior>
  <action>
1. In `src/validation/runner.ts`, add `lastOutput?: string` field to the `ValidationResult` interface.

2. In `runValidationLoop`, after running all steps in each pass, capture the combined output. Before both return paths (allPassed and exhausted retries), build `lastOutput` by concatenating all `results` entries' `stdout + stderr` (in step order, separated by newlines). Populate `lastOutput` on the returned `ValidationResult`. For the early-return `steps.length === 0` case, do NOT set lastOutput (leave undefined).

3. Create `src/pipeline/coverage.ts` with a single exported function:
```typescript
export function extractCoverage(output: string): number
```
Regex patterns in priority order:
- vitest: `/All files\s*\|\s*([\d.]+)/` — captures the summary percentage from vitest table
- jest/istanbul Statements: `/Statements\s*:\s*([\d.]+)%/`
- c8/istanbul Lines: `/Lines\s*:\s*([\d.]+)%/`
- Generic fallback: `/([\d.]+)%\s*coverage/i`

Return `parseFloat(match[1])` for the first match, or `-1` if none found.
  </action>
  <verify>
    <automated>cd /home/claude/forgectl-dev && FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/pipeline-self-correction.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>ValidationResult.lastOutput populated in both pass and fail paths. extractCoverage correctly parses vitest, jest, c8, and generic formats. Returns -1 when no pattern matches.</done>
</task>

<task type="auto">
  <name>Task 2: Create self-correction test scaffold with mock structure</name>
  <files>test/unit/pipeline-self-correction.test.ts</files>
  <action>
Create `test/unit/pipeline-self-correction.test.ts` with the same mock structure as `test/unit/pipeline-loop.test.ts` (mock loadConfig, executeRun, emitRunEvent, homedir).

**Important mock customization:** The `loadConfig` mock must include `repo: { exclude: ["test/**", "*.test.ts"] }` in its return value so exclusion enforcement tests have patterns to match against.

The `executeRun` mock must return `ValidationResult` with `lastOutput` populated so the executor can read test output for hash comparison and coverage parsing.

Include these test sections (with actual test implementations, not stubs):

1. **extractCoverage unit tests** (import from `../../src/pipeline/coverage.js`):
   - vitest format: `"All files | 72.34 | 50 | 80 | 72.34"` -> 72.34
   - jest/istanbul Statements: `"Statements   : 85.5% ( 100/117 )"` -> 85.5
   - c8/istanbul Lines: `"Lines        : 91.2% ( 200/219 )"` -> 91.2
   - Generic: `"91.2% coverage"` -> 91.2
   - No match: `"all tests passed"` -> -1
   - Empty string: `""` -> -1

2. **No-progress detection tests** (describe block, tests will call PipelineExecutor):
   - "aborts loop when consecutive iterations produce identical test output" — configure executeRun to return the same lastOutput string twice in a row. Verify the pipeline fails with error containing "no progress detected"
   - "continues loop when consecutive iterations produce different output" — configure executeRun to return different lastOutput each call. Verify loop runs all iterations (or until 'until' expression is true)
   - "does not trigger on first iteration" — single iteration with output should not trigger no-progress

3. **Exclusion enforcement tests** (describe block, tests mock execSync for git diff):
   - "fails iteration when fix agent modifies excluded file" — mock execSync to return "test/foo.test.ts" from git diff. Verify error contains "Fix agent modified excluded file(s): test/foo.test.ts"
   - "allows modification of non-excluded files" — mock execSync to return "src/main.ts". Verify iteration succeeds
   - "reverts excluded files via git checkout" — mock execSync, verify git checkout was called for violated files

4. **Coverage variable injection tests** (describe block):
   - "_coverage injected into until expression context" — configure executeRun with lastOutput containing coverage info, use until expression `_coverage >= 80`, verify loop exits when coverage meets threshold
   - "_coverage is -1 when no coverage in output" — configure executeRun with lastOutput lacking coverage, verify _coverage is -1 (loop continues)
   - "exhaustion message includes final coverage" — run loop to max_iterations with coverage below threshold, verify error contains "final coverage:"

For PipelineExecutor tests, follow the pattern from pipeline-loop.test.ts: create a PipelineDefinition with a loop node, instantiate PipelineExecutor, call execute(), and check nodeStates.

**Mock executeRun pattern for lastOutput:**
```typescript
vi.mocked(executeRun).mockResolvedValue({
  success: true,
  output: { mode: "files", dir: "/tmp/out", files: ["result.md"], totalSize: 100 },
  validation: { passed: true, totalAttempts: 1, stepResults: [], lastOutput: "All files | 72.34 | ..." },
  durationMs: 500,
});
```
  </action>
  <verify>
    <automated>cd /home/claude/forgectl-dev && FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/pipeline-self-correction.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Test file exists with passing extractCoverage tests and failing tests for no-progress, exclusion, and coverage injection (RED state for Plan 02 to make GREEN).</done>
</task>

</tasks>

<verification>
- `FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/pipeline-self-correction.test.ts` — extractCoverage tests pass
- `FORGECTL_SKIP_DOCKER=true npm test` — full suite still green (no regressions from ValidationResult change)
- `npx tsc --noEmit` — typecheck passes
</verification>

<success_criteria>
- ValidationResult.lastOutput field exists and is populated in runValidationLoop
- extractCoverage correctly parses 4 coverage formats and returns -1 for unrecognized output
- Test scaffold created with extractCoverage tests passing and executor integration tests ready for Plan 02
- Full test suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/24-self-correction-integration/24-01-SUMMARY.md`
</output>
