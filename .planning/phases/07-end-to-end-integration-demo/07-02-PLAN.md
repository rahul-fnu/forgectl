---
phase: 07-end-to-end-integration-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/backward-compat.test.ts
  - examples/code-review-workflow.md
autonomous: true
requirements: [NF1, R7.1]

must_haves:
  truths:
    - "forgectl run plan resolution still works with existing config"
    - "forgectl pipeline commands still work with existing config"
    - "Example WORKFLOW.md demonstrates full orchestrator configuration"
  artifacts:
    - path: "test/integration/backward-compat.test.ts"
      provides: "Backward compatibility verification tests"
      min_lines: 40
    - path: "examples/code-review-workflow.md"
      provides: "Example WORKFLOW.md for code review workflow"
      min_lines: 20
  key_links:
    - from: "test/integration/backward-compat.test.ts"
      to: "src/cli/run.ts"
      via: "imports and verifies plan resolution"
      pattern: "resolveRun|buildPlan|loadConfig"
---

<objective>
Create backward compatibility tests verifying that `forgectl run` and `forgectl pipeline` still work, and create an example WORKFLOW.md demonstrating orchestrator configuration with validation.

Purpose: Prove that the new orchestrator integration does not break existing functionality, and provide a reference WORKFLOW.md for users.
Output: Backward compat test file + example WORKFLOW.md with tracker, agent, validation, and prompt sections.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-end-to-end-integration-demo/07-RESEARCH.md

@src/cli/run.ts
@src/workflow/resolver.ts
@src/config/loader.ts
@src/workflow/workflow-file.ts
@test/integration/board-mixed-e2e.test.ts

<interfaces>
<!-- Key types for backward compat testing -->

From src/workflow/resolver.ts:
```typescript
export function resolveWorkflow(name: string, registry: WorkflowRegistry): WorkflowDefinition;
```

From src/config/loader.ts:
```typescript
export function loadConfig(configPath?: string): ForgectlConfig;
```

From src/workflow/workflow-file.ts:
```typescript
export function parseWorkflowFile(content: string): { frontMatter: Record<string, unknown>; body: string };
export const WorkflowFrontMatterSchema: z.ZodObject<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backward compatibility tests for run and pipeline commands</name>
  <files>test/integration/backward-compat.test.ts</files>
  <behavior>
    - Test: loadConfig() with a minimal forgectl.yaml (no orchestrator section) succeeds and returns valid defaults
    - Test: resolveWorkflow("code") returns a valid workflow definition (built-in workflow still works)
    - Test: parseWorkflowFile with a WORKFLOW.md that has NO validation section still parses correctly
    - Test: WorkflowFrontMatterSchema.parse({}) succeeds (all fields optional)
    - Test: existing forgectl.yaml configs with only agent/container/repo sections are still valid
    - Test: pipeline parser still accepts existing pipeline YAML format
  </behavior>
  <action>
Create `test/integration/backward-compat.test.ts` that verifies:

1. **Config backward compat:** `loadConfig` with a minimal config (just agent.type + container.image) produces valid ForgectlConfig with correct defaults. No orchestrator section required. Mock the file read to provide minimal YAML.

2. **Workflow resolution backward compat:** `resolveWorkflow("code")` returns a valid WorkflowDefinition. The built-in workflow registry is unmodified.

3. **WORKFLOW.md backward compat:** `parseWorkflowFile` and `WorkflowFrontMatterSchema.parse` accept front matter without a validation section. The new validation field is optional and defaults correctly.

4. **Pipeline backward compat:** `parsePipelineConfig` still accepts the existing pipeline YAML format (import from `src/pipeline/parser.ts`). Use a minimal pipeline YAML with stages and steps.

Use mock file reads where needed (vi.mock node:fs). Pattern after existing integration tests in the project.

Do NOT import or test anything that requires Docker. All checks should be config/schema/parsing level.
  </action>
  <verify>
    <automated>FORGECTL_SKIP_DOCKER=true npx vitest run test/integration/backward-compat.test.ts -x</automated>
  </verify>
  <done>All backward compatibility tests pass. Config, workflow resolution, WORKFLOW.md parsing, and pipeline parsing work unchanged with existing formats.</done>
</task>

<task type="auto">
  <name>Task 2: Example WORKFLOW.md for code review workflow</name>
  <files>examples/code-review-workflow.md</files>
  <action>
Create `examples/code-review-workflow.md` with:

1. **YAML front matter** demonstrating all orchestrator-relevant config sections:
   ```yaml
   tracker:
     kind: github
     token: $GITHUB_TOKEN
     repo: owner/repo
     labels: [forgectl]
     active_states: [open]
     terminal_states: [closed]
     auto_close: true
     done_label: done
     in_progress_label: in-progress
   polling:
     interval_ms: 30000
   concurrency:
     max_agents: 2
   workspace:
     root: ~/.forgectl/workspaces
     hooks:
       after_create: "git clone https://github.com/owner/repo.git ."
       before_run: "git checkout main && git pull"
   agent:
     type: claude-code
     model: sonnet
     timeout: 30m
   validation:
     steps:
       - name: typecheck
         command: npm run typecheck
         retries: 2
       - name: test
         command: npm test
         retries: 3
       - name: lint
         command: npm run lint
         retries: 1
     on_failure: abandon
   ```

2. **Prompt template body** with template variables:
   ```markdown
   You are a code review assistant. Implement the changes requested in this issue.

   ## Issue: {{issue.title}}

   {{issue.description}}

   ## Instructions
   - Work in the /workspace directory
   - Make targeted changes to address the issue
   - Ensure all existing tests still pass
   - Follow the project's coding conventions
   - Commit your changes with a descriptive message

   {{#if attempt}}
   This is retry attempt {{attempt}}. Review previous feedback and fix any remaining issues.
   {{/if}}
   ```

3. Add a comment header explaining this is an example WORKFLOW.md for the forgectl orchestrator.
  </action>
  <verify>
    <automated>node -e "const { parseWorkflowFile, WorkflowFrontMatterSchema } = require('./dist/workflow/workflow-file.js'); const fs = require('fs'); const content = fs.readFileSync('examples/code-review-workflow.md', 'utf8'); const { frontMatter } = parseWorkflowFile(content); WorkflowFrontMatterSchema.parse(frontMatter); console.log('OK: Example WORKFLOW.md validates successfully');" 2>/dev/null || npx tsx -e "import { parseWorkflowFile, WorkflowFrontMatterSchema } from './src/workflow/workflow-file.js'; import { readFileSync } from 'fs'; const content = readFileSync('examples/code-review-workflow.md', 'utf8'); const { frontMatter } = parseWorkflowFile(content); WorkflowFrontMatterSchema.parse(frontMatter); console.log('OK: Example WORKFLOW.md validates successfully');"</automated>
  </verify>
  <done>Example WORKFLOW.md exists in examples/ directory, demonstrates all orchestrator config sections including validation, and passes front matter schema validation.</done>
</task>

</tasks>

<verification>
FORGECTL_SKIP_DOCKER=true npx vitest run
All tests pass including new backward-compat tests.
Example WORKFLOW.md validates against the schema.
</verification>

<success_criteria>
- Backward compat tests prove run/pipeline commands still work
- Example WORKFLOW.md has tracker, agent, workspace, validation, and prompt sections
- Example validates against WorkflowFrontMatterSchema
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-end-to-end-integration-demo/07-02-SUMMARY.md`
</output>
