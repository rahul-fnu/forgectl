---
phase: 08-wire-workflow-runtime-integration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - test/unit/daemon-integration.test.ts
autonomous: true
requirements: [R4.3, R4.4]

must_haves:
  truths:
    - "Full reload cycle works end-to-end: WORKFLOW.md change triggers re-merge and applyConfig"
    - "Claude Code adapter config is correctly threaded through merged config"
    - "Codex adapter config is correctly threaded through merged config (mocked)"
    - "Poll interval change in WORKFLOW.md takes effect on next scheduler tick"
    - "Concurrency change in WORKFLOW.md updates SlotManager max"
  artifacts:
    - path: "test/unit/daemon-integration.test.ts"
      provides: "Integration tests for watcher + merge + orchestrator + adapters"
      min_lines: 80
  key_links:
    - from: "test/unit/daemon-integration.test.ts"
      to: "src/orchestrator/index.ts"
      via: "Tests applyConfig with agent-specific config scenarios"
      pattern: "applyConfig"
    - from: "test/unit/daemon-integration.test.ts"
      to: "src/workflow/map-front-matter.ts"
      via: "Tests full mapping + merge + apply pipeline"
      pattern: "mapFrontMatterToConfig"
---

<objective>
Integration tests verifying the full watcher-reload-merge-apply pipeline works correctly with both agent adapter types.

Purpose: Ensure the wiring from Plan 01 correctly propagates WORKFLOW.md changes through the entire config pipeline to the Orchestrator, including agent-specific config scenarios.

Output: Integration test file covering end-to-end reload scenarios.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wire-workflow-runtime-integration/08-RESEARCH.md
@.planning/phases/08-wire-workflow-runtime-integration/08-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan tests against -->

From src/orchestrator/index.ts (added in Plan 01):
```typescript
export class Orchestrator {
  applyConfig(config: ForgectlConfig, promptTemplate: string): void;
}
```

From src/orchestrator/state.ts (added in Plan 01):
```typescript
export class SlotManager {
  setMax(n: number): void;
}
```

From src/workflow/map-front-matter.ts (created in Plan 01):
```typescript
export function mapFrontMatterToConfig(fm: WorkflowFileConfig): Partial<ForgectlConfig>;
```

From src/workflow/merge.ts (existing):
```typescript
export function mergeWorkflowConfig(
  defaults: ForgectlConfig,
  forgectlYaml: Partial<ForgectlConfig>,
  workflowFrontMatter: Partial<ForgectlConfig>,
  cliFlags: Partial<ForgectlConfig>,
): ForgectlConfig;
```

From src/workflow/types.ts:
```typescript
export interface WorkflowFileConfig { ... }
export interface ValidatedWorkflowFile { config: WorkflowFileConfig; promptTemplate: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for full reload pipeline with agent adapter scenarios</name>
  <files>test/unit/daemon-integration.test.ts</files>
  <behavior>
    - Full pipeline: WorkflowFileConfig -> mapFrontMatterToConfig -> mergeWorkflowConfig -> applyConfig produces correct merged config
    - Changing agent.type from claude-code to codex in WORKFLOW.md front matter is reflected in merged config
    - Changing agent.model in front matter overrides yaml agent.model
    - Changing polling.interval_ms in front matter updates orchestrator.poll_interval_ms in merged config
    - Changing concurrency.max_agents in front matter updates orchestrator.max_concurrent_agents in merged config
    - Prompt template from WORKFLOW.md body is passed through to applyConfig
    - Multiple sequential reloads each produce correct merged config (no stale state)
    - Front matter with only tracker overrides does not affect orchestrator or agent config
    - Empty front matter reload keeps yaml + defaults as merged config
    - Validation config from front matter merges correctly
  </behavior>
  <action>
    Create `test/unit/daemon-integration.test.ts` that tests the full pipeline without starting Fastify or Docker:

    1. **Setup helper:** Create a `simulateReload` function that:
       - Takes `defaults`, `yamlConfig`, `WorkflowFileConfig`, `cliFlags`
       - Calls `mapFrontMatterToConfig(fm)` then `mergeWorkflowConfig(defaults, yaml, mapped, cli)`
       - Returns the merged `ForgectlConfig`
       - This mirrors exactly what server.ts does in the onReload callback

    2. **Test: Full reload pipeline with Claude Code config**
       - yaml config has `agent.type: "claude-code"`, `orchestrator.poll_interval_ms: 30000`
       - Front matter has `polling: { interval_ms: 15000 }`
       - Verify merged config has `orchestrator.poll_interval_ms: 15000` and `agent.type: "claude-code"` preserved

    3. **Test: Full reload pipeline with Codex config**
       - yaml config has `agent.type: "codex"`, `orchestrator.max_concurrent_agents: 3`
       - Front matter has `concurrency: { max_agents: 5 }`, `agent: { model: "codex-mini" }`
       - Verify merged config has `orchestrator.max_concurrent_agents: 5` and `agent.model: "codex-mini"`

    4. **Test: Prompt template passthrough**
       - Create a ValidatedWorkflowFile with custom promptTemplate
       - Simulate reload, verify promptTemplate is the body string (not from config)

    5. **Test: Multiple sequential reloads**
       - Reload 1: front matter sets polling.interval_ms to 10000
       - Reload 2: front matter changes to 20000
       - Verify each reload produces correct merged config independently

    6. **Test: Orchestrator.applyConfig integration**
       - Create Orchestrator with mocked deps (mock tracker, workspaceManager, logger)
       - Call start() to initialize deps
       - Call applyConfig with new merged config
       - Verify deps.config and deps.promptTemplate updated
       - Verify slotManager.getMax() updated if concurrency changed
       - Call stop() to clean up

    7. **Test: Partial front matter (tracker only)**
       - Front matter has only `tracker: { labels: ["ai-task"] }`
       - Verify orchestrator and agent config unchanged from yaml + defaults

    8. **Test: Validation config from front matter**
       - Front matter has `validation: { steps: [{ name: "lint", command: "npm run lint" }], on_failure: "abandon" }`
       - Verify merged config includes validation steps

    All tests use real `mergeWorkflowConfig`, real `mapFrontMatterToConfig`, and real `ConfigSchema.parse({})` for defaults. Only tracker, workspaceManager, and logger are mocked.
  </action>
  <verify>
    <automated>FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/daemon-integration.test.ts -x</automated>
  </verify>
  <done>Integration tests cover full reload pipeline with both agent types, sequential reloads, partial overrides, and Orchestrator.applyConfig integration. Full suite passes: `FORGECTL_SKIP_DOCKER=true npm test`.</done>
</task>

</tasks>

<verification>
- `FORGECTL_SKIP_DOCKER=true npm test` — full test suite passes (all existing + new tests)
- `npx tsc --noEmit` — no type errors
- Integration tests verify the complete pipeline: WorkflowFileConfig -> map -> merge -> applyConfig
</verification>

<success_criteria>
- Full reload pipeline tested end-to-end (map + merge + apply)
- Both Claude Code and Codex agent config scenarios covered
- Sequential reloads produce correct independent results
- Partial front matter does not corrupt other config sections
- Orchestrator.applyConfig correctly updates deps and SlotManager
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-wire-workflow-runtime-integration/08-02-SUMMARY.md`
</output>
