---
phase: 16-wire-flight-recorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/daemon/server.ts
  - test/unit/daemon-recorder-wiring.test.ts
autonomous: true
requirements: [AUDT-01, AUDT-03]
gap_closure: true

must_haves:
  truths:
    - "Events emitted by emitRunEvent() are persisted to SQLite via EventRecorder"
    - "forgectl run inspect <id> returns actual event data from the database"
    - "EventRecorder listener is removed on daemon shutdown before database closes"
  artifacts:
    - path: "src/daemon/server.ts"
      provides: "EventRecorder instantiation and shutdown cleanup"
      contains: "EventRecorder"
    - path: "test/unit/daemon-recorder-wiring.test.ts"
      provides: "Verification that EventRecorder is wired in server.ts"
  key_links:
    - from: "src/daemon/server.ts"
      to: "src/logging/recorder.ts"
      via: "new EventRecorder(eventRepo, snapshotRepo)"
      pattern: "new EventRecorder"
    - from: "src/logging/recorder.ts"
      to: "src/logging/events.ts"
      via: "runEvents.on('run', handler) in constructor"
      pattern: "runEvents\\.on"
    - from: "src/daemon/server.ts"
      to: "src/storage/repositories/events.ts"
      via: "createEventRepository(db)"
      pattern: "createEventRepository"
---

<objective>
Wire EventRecorder into the daemon so the flight recorder audit trail actually persists events to the database.

Purpose: EventRecorder exists and is tested but was never instantiated in startDaemon(). All 39 emitRunEvent() call sites fire events into the void. This single plan closes both AUDT-01 (event persistence) and AUDT-03 (inspect reads real data).
Output: Modified server.ts with EventRecorder wired, plus a wiring verification test.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wire-flight-recorder/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/logging/recorder.ts:
```typescript
export class EventRecorder {
  constructor(eventRepo: EventRepository, snapshotRepo: SnapshotRepository);
  close(): void;
}
```

From src/storage/repositories/events.ts:
```typescript
export function createEventRepository(db: BetterSqlite3Database): EventRepository;
```

From src/daemon/server.ts (existing repos already created):
```typescript
// Line 48-49: these already exist
const snapshotRepo = createSnapshotRepository(db);
const lockRepo = createLockRepository(db);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test + Wire EventRecorder in server.ts</name>
  <files>test/unit/daemon-recorder-wiring.test.ts, src/daemon/server.ts</files>
  <behavior>
    - Test 1: server.ts source code imports createEventRepository from storage/repositories/events
    - Test 2: server.ts source code imports EventRecorder from logging/recorder
    - Test 3: server.ts source code contains `new EventRecorder(` instantiation
    - Test 4: server.ts source code calls `recorder.close()` before `closeDatabase`
  </behavior>
  <action>
    1. Create `test/unit/daemon-recorder-wiring.test.ts` that reads `src/daemon/server.ts` as source text and verifies:
       - Import of `createEventRepository` from `../storage/repositories/events.js`
       - Import of `EventRecorder` from `../logging/recorder.js`
       - Instantiation: `new EventRecorder(`
       - Shutdown cleanup: `recorder.close()` appears before `closeDatabase`

    2. Modify `src/daemon/server.ts`:
       - Add import: `import { createEventRepository } from "../storage/repositories/events.js";`
       - Add import: `import { EventRecorder } from "../logging/recorder.js";`
       - After line 49 (after `const lockRepo = createLockRepository(db);`), add:
         ```typescript
         const eventRepo = createEventRepository(db);
         const recorder = new EventRecorder(eventRepo, snapshotRepo);
         ```
       - In the `shutdown` async function, add `recorder.close();` BEFORE `closeDatabase(db);`

    IMPORTANT: EventRecorder constructor auto-subscribes to runEvents EventEmitter. No manual subscription needed. Just instantiate before any runs start (before RunQueue creation on line 67).
  </action>
  <verify>
    <automated>FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/daemon-recorder-wiring.test.ts test/unit/event-recorder.test.ts -x</automated>
  </verify>
  <done>
    - server.ts imports createEventRepository and EventRecorder
    - EventRecorder instantiated after repo creation, before RunQueue
    - recorder.close() called in shutdown before closeDatabase
    - Wiring test passes confirming all 4 checks
    - Existing event-recorder unit tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification and typecheck</name>
  <files>src/daemon/server.ts</files>
  <action>
    Run the full test suite and typecheck to confirm no regressions from the server.ts changes.
    If typecheck or tests fail, fix any issues (likely import path or type mismatches).
  </action>
  <verify>
    <automated>FORGECTL_SKIP_DOCKER=true npm test && npm run typecheck</automated>
  </verify>
  <done>
    - Full test suite passes (667+ tests)
    - TypeScript compilation succeeds with no errors
    - No regressions introduced
  </done>
</task>

</tasks>

<verification>
1. `FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/daemon-recorder-wiring.test.ts` — wiring test green
2. `FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/event-recorder.test.ts` — existing recorder tests green
3. `FORGECTL_SKIP_DOCKER=true npm test` — full suite green
4. `npm run typecheck` — no type errors
</verification>

<success_criteria>
- EventRecorder is instantiated in startDaemon() and auto-subscribes to runEvents
- Events emitted via emitRunEvent() will be persisted to SQLite
- `forgectl run inspect <id>` will return real event data (not empty)
- recorder.close() is called during shutdown before database closes
- All tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-wire-flight-recorder/16-01-SUMMARY.md`
</output>
