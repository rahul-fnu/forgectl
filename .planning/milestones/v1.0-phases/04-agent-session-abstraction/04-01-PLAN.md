---
phase: 04-agent-session-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/session.ts
  - src/agent/oneshot-session.ts
  - test/unit/session.test.ts
autonomous: true
requirements: [R5.1, R5.2, R5.4]

must_haves:
  truths:
    - "AgentSession interface defines invoke/isAlive/close contract"
    - "OneShotSession wraps invokeAgent and returns structured AgentResult"
    - "createAgentSession factory returns OneShotSession for claude-code and basic codex"
    - "Activity callback fires when agent produces output"
    - "Invoking after close throws an error"
    - "Token usage defaults to zeros for one-shot mode"
  artifacts:
    - path: "src/agent/session.ts"
      provides: "AgentSession interface, AgentResult type, AgentStatus enum, TokenUsage type, createAgentSession factory"
      exports: ["AgentSession", "AgentResult", "AgentStatus", "TokenUsage", "AgentSessionOptions", "createAgentSession"]
    - path: "src/agent/oneshot-session.ts"
      provides: "OneShotSession class implementing AgentSession"
      exports: ["OneShotSession"]
    - path: "test/unit/session.test.ts"
      provides: "Unit tests for session interface, factory, one-shot lifecycle, activity tracking"
  key_links:
    - from: "src/agent/oneshot-session.ts"
      to: "src/agent/invoke.ts"
      via: "invokeAgent() delegation"
      pattern: "invokeAgent\\("
    - from: "src/agent/session.ts"
      to: "src/agent/registry.ts"
      via: "getAgentAdapter() in factory"
      pattern: "getAgentAdapter\\("
---

<objective>
Define the AgentSession interface, AgentResult type, and OneShotSession implementation with factory function.

Purpose: Establish the session abstraction that unifies one-shot and persistent agent modes. OneShotSession wraps existing invokeAgent() to maintain backward compatibility while adding lifecycle semantics (isAlive/close) and structured results (status enum, tokenUsage, durationMs, turnCount).

Output: Session types, OneShotSession class, factory function, and comprehensive unit tests.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-agent-session-abstraction/04-CONTEXT.md
@.planning/phases/04-agent-session-abstraction/04-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/agent/types.ts:
```typescript
export interface AgentAdapter {
  name: string;
  buildShellCommand(promptFile: string, options: AgentOptions): string;
}

export interface AgentOptions {
  model: string;
  maxTurns: number;
  timeout: number;     // ms
  flags: string[];
  workingDir: string;
}
```

From src/agent/invoke.ts:
```typescript
export async function invokeAgent(
  container: Docker.Container,
  adapter: AgentAdapter,
  prompt: string,
  options: AgentOptions,
  env?: string[],
  promptId?: string,
): Promise<ExecResult>;
```

From src/container/runner.ts:
```typescript
export interface ExecResult {
  exitCode: number;
  stdout: string;
  stderr: string;
  durationMs: number;
}
```

From src/agent/registry.ts:
```typescript
export function getAgentAdapter(name: string): AgentAdapter;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentSession interface, AgentResult types, and OneShotSession</name>
  <files>src/agent/session.ts, src/agent/oneshot-session.ts, test/unit/session.test.ts</files>
  <behavior>
    - Factory returns OneShotSession for "claude-code" agent type
    - Factory returns OneShotSession for "codex" agent type (when no app-server config)
    - Factory throws for unknown agent type (delegates to getAgentAdapter which throws)
    - OneShotSession.invoke() delegates to invokeAgent() and returns AgentResult
    - AgentResult.status is "completed" when exit code is 0
    - AgentResult.status is "failed" when exit code is non-zero
    - AgentResult.tokenUsage defaults to { input: 0, output: 0, total: 0 }
    - AgentResult.turnCount is always 1 for one-shot
    - AgentResult.durationMs comes from ExecResult.durationMs
    - AgentResult.stdout and stderr come from ExecResult
    - OneShotSession.isAlive() returns true initially, false after close()
    - OneShotSession.invoke() throws "Session is closed" after close()
    - Activity callback fires when invokeAgent returns output (stdout or stderr non-empty)
    - Activity callback does NOT fire when both stdout and stderr are empty
    - OneShotSession without onActivity callback works fine (no crash)
    - Timeout option on invoke() is forwarded to invokeAgent via AgentOptions
  </behavior>
  <action>
  Create `src/agent/session.ts` with:
  - `AgentStatus` type: `"completed" | "failed" | "timeout" | "user_input_required"`
  - `TokenUsage` interface: `{ input: number; output: number; total: number }`
  - `AgentResult` interface: `{ stdout, stderr, status: AgentStatus, tokenUsage: TokenUsage, durationMs, turnCount }`
  - `AgentSessionOptions` interface: `{ onActivity?: () => void }`
  - `AgentSession` interface: `invoke(prompt, options?) => Promise<AgentResult>`, `isAlive() => boolean`, `close() => Promise<void>`
  - `createAgentSession(agentType, container, agentOptions, env, sessionOptions?)` factory function
  - Factory uses `getAgentAdapter(agentType)` internally and returns `new OneShotSession(...)` (AppServerSession branch added in plan 02)

  Create `src/agent/oneshot-session.ts` with:
  - `OneShotSession` class implementing `AgentSession`
  - Constructor takes: `container: Docker.Container`, `adapter: AgentAdapter`, `agentOptions: AgentOptions`, `env: string[]`, `sessionOptions?: AgentSessionOptions`
  - Private `alive` boolean, starts `true`
  - `invoke()`: guards on alive, calls `invokeAgent(container, adapter, prompt, opts, env)`, fires `onActivity` if stdout or stderr non-empty, returns mapped `AgentResult`
  - Helper `mapExitCodeToStatus(exitCode: number): AgentStatus` — 0 = "completed", else "failed"
  - If `invoke()` options has `timeout`, create a modified `AgentOptions` with that timeout
  - `isAlive()`: returns `this.alive`
  - `close()`: sets `this.alive = false`

  Create `test/unit/session.test.ts` with mocked `invokeAgent` and `getAgentAdapter`:
  - Use `vi.mock("../src/agent/invoke.js")` and `vi.mock("../src/agent/registry.js")`
  - Test all behaviors listed above
  - Mock container as a plain object (only passed through, not called directly)
  </action>
  <verify>
    <automated>cd /home/claude/forgectl && FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/session.test.ts</automated>
  </verify>
  <done>
  - AgentSession interface exported from session.ts with invoke/isAlive/close
  - AgentResult, AgentStatus, TokenUsage types exported
  - createAgentSession factory returns OneShotSession for known agent types
  - OneShotSession wraps invokeAgent correctly
  - Activity callback fires on output
  - Close prevents further invocations
  - All tests pass
  </done>
</task>

</tasks>

<verification>
- `FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/session.test.ts` — all tests pass
- `npx tsc --noEmit` — no type errors
- `FORGECTL_SKIP_DOCKER=true npm test` — full suite still passes (no regressions)
</verification>

<success_criteria>
- AgentSession interface is the single contract for all agent invocations
- OneShotSession produces correct AgentResult from ExecResult
- Factory creates sessions by agent type
- Activity tracking callback pattern works
- Session lifecycle (alive/close) enforced
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-session-abstraction/04-01-SUMMARY.md`
</output>
