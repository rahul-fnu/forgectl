---
phase: 03-workflow-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow/workflow-file.ts
  - src/workflow/template.ts
  - src/workflow/types.ts
  - test/unit/workflow-file.test.ts
  - test/unit/workflow-template.test.ts
autonomous: true
requirements: [R4.1, R4.2]

must_haves:
  truths:
    - "WORKFLOW.md with YAML front matter and markdown body is parsed into config object + prompt template string"
    - "Missing WORKFLOW.md throws an error"
    - "Empty prompt body falls back to default prompt template"
    - "Front matter is validated with zod and unknown keys are rejected"
    - "Prompt template renders issue fields via {{issue.*}} and {{attempt}} variables"
    - "Unknown template variables cause a render error (strict mode)"
    - "Arrays in template variables render as JSON arrays"
    - "Null values in template variables render as empty string"
    - "Attempt renders as empty string on first run (null)"
  artifacts:
    - path: "src/workflow/workflow-file.ts"
      provides: "WORKFLOW.md parser with front matter extraction, zod validation, and file loading"
      exports: ["parseWorkflowFile", "loadWorkflowFile", "WorkflowFrontMatterSchema", "DEFAULT_PROMPT_TEMPLATE"]
    - path: "src/workflow/template.ts"
      provides: "Strict prompt template renderer with issue-aware variable map builder"
      exports: ["renderPromptTemplate", "buildTemplateVars"]
    - path: "src/workflow/types.ts"
      provides: "WorkflowFileConfig and ValidatedWorkflowFile types"
      contains: "WorkflowFileConfig"
    - path: "test/unit/workflow-file.test.ts"
      provides: "Unit tests for WORKFLOW.md parsing and validation"
    - path: "test/unit/workflow-template.test.ts"
      provides: "Unit tests for strict template rendering"
  key_links:
    - from: "src/workflow/workflow-file.ts"
      to: "js-yaml"
      via: "yaml.load() for front matter parsing"
      pattern: "yaml\\.load"
    - from: "src/workflow/workflow-file.ts"
      to: "src/workflow/workflow-file.ts"
      via: "WorkflowFrontMatterSchema.parse() for validation"
      pattern: "WorkflowFrontMatterSchema\\.parse"
    - from: "src/workflow/template.ts"
      to: "src/tracker/types.ts"
      via: "TrackerIssue type for buildTemplateVars input"
      pattern: "TrackerIssue"
---

<objective>
Parse WORKFLOW.md files (YAML front matter + markdown body) and render prompt templates with issue data.

Purpose: Provides the core file format and template rendering that the watcher and config merge (Plan 02) build upon.
Output: Parser, validator, template renderer, and comprehensive tests.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-contract/03-CONTEXT.md
@.planning/phases/03-workflow-contract/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/tracker/types.ts:
```typescript
export interface TrackerIssue {
  id: string;
  identifier: string;
  title: string;
  description: string;
  state: string;
  priority: string | null;
  labels: string[];
  assignees: string[];
  url: string;
  created_at: string;
  updated_at: string;
  blocked_by: string[];
  metadata: Record<string, unknown>;
}
```

From src/config/schema.ts:
```typescript
export const AgentType = z.enum(["claude-code", "codex"]);
export const TrackerConfigSchema = z.object({ ... }).superRefine(...);
export const WorkspaceConfigSchema = z.object({ ... });
export const WorkflowSchema = z.object({
  name: z.string(),
  extends: z.string().optional(),
  // ...
});
export type ForgectlConfig = z.infer<typeof ConfigSchema>;
```

From src/utils/template.ts:
```typescript
// NOTE: Do NOT modify this function. It leaves unresolved vars as-is (by design).
// Create a NEW strict function in src/workflow/template.ts instead.
export function expandTemplate(template: string, vars: Record<string, unknown>): string;
```

From src/workflow/types.ts (existing, will be extended):
```typescript
export type { WorkflowDefinition, AgentType, ValidationStep };
export interface NetworkConfig { ... }
export interface RunPlan { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WORKFLOW.md parser and front matter schema</name>
  <files>src/workflow/workflow-file.ts, src/workflow/types.ts, test/unit/workflow-file.test.ts</files>
  <behavior>
    - parseWorkflowFile("---\nextends: code\ntracker:\n  poll_interval_ms: 5000\n---\nDo {{issue.title}}") returns { frontMatter: { extends: "code", tracker: { poll_interval_ms: 5000 } }, body: "Do {{issue.title}}" }
    - parseWorkflowFile("no front matter") throws Error with message about missing --- delimiters
    - parseWorkflowFile("---\n---\n") returns { frontMatter: {}, body: "" } (empty front matter is valid per C3)
    - parseWorkflowFile with body containing --- horizontal rules after front matter does NOT split incorrectly
    - WorkflowFrontMatterSchema.parse({ extends: "code" }) succeeds
    - WorkflowFrontMatterSchema.parse({ unknown_key: true }) throws ZodError (strict mode)
    - WorkflowFrontMatterSchema.parse({ tracker: { kind: "github", token: "$GH", repo: "o/r" } }) validates tracker partial
    - loadWorkflowFile("/nonexistent") throws Error (C2: missing file is an error)
    - loadWorkflowFile with empty body returns DEFAULT_PROMPT_TEMPLATE as promptTemplate
    - loadWorkflowFile with valid file returns { config: validated, promptTemplate: body }
  </behavior>
  <action>
    1. Extend `src/workflow/types.ts` with:
       - `WorkflowFileConfig` type (output of zod parse of front matter)
       - `ValidatedWorkflowFile` interface: `{ config: WorkflowFileConfig; promptTemplate: string }`

    2. Create `src/workflow/workflow-file.ts`:
       - `parseWorkflowFile(content: string): { frontMatter: Record<string, unknown>; body: string }` — regex splits on first `---` at position 0, non-greedy match to closing `---`. Throws if no front matter delimiters found.
       - `WorkflowFrontMatterSchema` — zod schema with `.strict()`:
         - `extends`: z.string().optional()
         - `tracker`: TrackerConfigSchema.partial() with no superRefine (use z.object with same fields but all optional, omit superRefine since these are overrides)
         - `polling`: z.object({ interval_ms: z.number().int().positive() }).optional()
         - `concurrency`: z.object({ max_agents: z.number().int().positive() }).optional()
         - `workspace`: WorkspaceConfigSchema.partial().optional() — but note WorkspaceConfigSchema has defaults, so use a separate partial schema
         - `agent`: z.object({ type: AgentType, model: z.string(), timeout: duration }).partial().optional()
         NOTE: For tracker, create a partial version WITHOUT the superRefine, since front matter provides overrides not complete configs. Use z.object with same fields as TrackerConfigSchema but all .optional() and no superRefine.
       - `DEFAULT_PROMPT_TEMPLATE` constant per CONTEXT.md B4
       - `loadWorkflowFile(filePath: string): Promise<ValidatedWorkflowFile>` — reads file, parses, validates front matter with schema, uses body or DEFAULT_PROMPT_TEMPLATE

    3. Create `test/unit/workflow-file.test.ts` with tests matching behavior above.

    IMPORTANT: The front matter regex must only match `---` at the very START of the file (position 0). Use `^---\r?\n` at the start of the pattern. The closing `---` uses non-greedy match.
  </action>
  <verify>
    <automated>npx vitest run test/unit/workflow-file.test.ts</automated>
  </verify>
  <done>parseWorkflowFile parses YAML front matter + body, validates with zod strict schema, loadWorkflowFile reads from disk and returns validated config + prompt template. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strict prompt template renderer</name>
  <files>src/workflow/template.ts, test/unit/workflow-template.test.ts</files>
  <behavior>
    - renderPromptTemplate("Hello {{issue.title}}", { issue: { title: "Fix bug" } }) returns "Hello Fix bug"
    - renderPromptTemplate("{{issue.labels}}", { issue: { labels: ["bug", "forgectl"] } }) returns '["bug","forgectl"]' (B1: JSON array)
    - renderPromptTemplate("{{issue.priority}}", { issue: { priority: null } }) returns "" (B2: null = empty)
    - renderPromptTemplate("{{attempt}}", { attempt: "" }) returns "" (B3: first run)
    - renderPromptTemplate("{{attempt}}", { attempt: 2 }) returns "2" (retry)
    - renderPromptTemplate("{{unknown}}", { issue: {} }) throws Error with "Unknown template variable: {{unknown}}"
    - renderPromptTemplate("{{issue.nonexistent}}", { issue: { title: "x" } }) throws Error
    - buildTemplateVars(trackerIssue, null) returns { issue: { ...fields }, attempt: "" }
    - buildTemplateVars(trackerIssue, 3) returns { issue: { ...fields }, attempt: 3 }
    - buildTemplateVars with issue.priority null maps to "" in result
  </behavior>
  <action>
    1. Create `src/workflow/template.ts`:
       - `renderPromptTemplate(template: string, vars: Record<string, unknown>): string` — strict mode template expansion. Uses same `{{key.path}}` regex as expandTemplate but:
         - Throws on unresolved variables (instead of leaving as-is)
         - Serializes arrays with JSON.stringify (B1)
         - Serializes null/undefined as "" (B2)
       - `buildTemplateVars(issue: TrackerIssue, attempt: number | null): Record<string, unknown>` — builds the variable map:
         - Nests all TrackerIssue fields under `issue.*`
         - Maps `attempt: null` to `attempt: ""` (B3)
         - Maps `priority: null` to `priority: ""` (B2 for null fields within issue)

    2. Create `test/unit/workflow-template.test.ts` with tests matching behavior above. Import TrackerIssue type and create a fixture issue for buildTemplateVars tests.

    IMPORTANT: Do NOT modify `src/utils/template.ts`. The existing `expandTemplate` has different semantics (leaves unresolved as-is) and is used elsewhere. This is a separate function.
  </action>
  <verify>
    <automated>npx vitest run test/unit/workflow-template.test.ts</automated>
  </verify>
  <done>renderPromptTemplate renders with strict mode (throws on unknown vars, JSON arrays, null-to-empty). buildTemplateVars maps TrackerIssue + attempt to template variable map. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run test/unit/workflow-file.test.ts test/unit/workflow-template.test.ts
</verification>

<success_criteria>
- WORKFLOW.md files parse correctly: front matter extracted as validated config, body as prompt template
- Front matter validation rejects unknown keys (strict mode)
- Missing file throws, empty body uses default prompt
- Template rendering is strict: unknown vars throw, arrays serialize as JSON, null becomes empty string
- All unit tests pass
- No modifications to existing expandTemplate
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-contract/03-01-SUMMARY.md`
</output>
