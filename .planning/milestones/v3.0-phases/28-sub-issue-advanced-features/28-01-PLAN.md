---
phase: 28-sub-issue-advanced-features
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/github/sub-issue-rollup.ts
  - test/unit/github-sub-issue-rollup.test.ts
autonomous: true
requirements: [SUBISSUE-05, SUBISSUE-06]

must_haves:
  truths:
    - "buildSubIssueProgressComment renders markdown checklist with status emojis, issue links, and aggregate footer"
    - "findRollupCommentId paginates through issue comments and finds the hidden HTML marker"
    - "upsertRollupComment creates when no marker exists and updates when it does"
    - "allChildrenTerminal returns true only when every child is in a terminal state and false for empty maps"
  artifacts:
    - path: "src/github/sub-issue-rollup.ts"
      provides: "RollupOctokitLike interface, ChildStatus type, marker builder, comment search, upsert, progress comment builder, all-terminal check"
      exports: ["RollupOctokitLike", "ChildStatus", "buildRollupMarker", "findRollupCommentId", "upsertRollupComment", "buildSubIssueProgressComment", "allChildrenTerminal"]
    - path: "test/unit/github-sub-issue-rollup.test.ts"
      provides: "Unit tests for all exports"
      min_lines: 100
  key_links:
    - from: "src/github/sub-issue-rollup.ts"
      to: "RollupOctokitLike"
      via: "Minimal Octokit interface for listComments/createComment/updateComment"
      pattern: "RollupOctokitLike"
---

<objective>
Create the standalone sub-issue rollup module with all pure functions for progress comment building, marker-based comment search, upsert logic, and all-children-terminal check.

Purpose: Provide the building blocks that Plan 02 will wire into the dispatcher. All functions are testable in isolation with mock Octokit.
Output: `src/github/sub-issue-rollup.ts` with full unit test coverage.
</objective>

<execution_context>
@/home/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-sub-issue-advanced-features/28-CONTEXT.md
@.planning/phases/28-sub-issue-advanced-features/28-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/github/comments.ts (existing OctokitLike pattern to follow):
```typescript
interface OctokitLike {
  rest: {
    issues: {
      createComment(params: {
        owner: string; repo: string; issue_number: number; body: string;
      }): Promise<{ data: { id: number } }>;
      updateComment(params: {
        owner: string; repo: string; comment_id: number; body: string;
      }): Promise<unknown>;
    };
  };
}
```

From src/tracker/sub-issue-cache.ts (data source for rollup):
```typescript
export interface SubIssueEntry {
  parentId: string;
  childIds: string[];
  childStates: Map<string, string>; // childId -> "open"/"closed"
  fetchedAt: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sub-issue-rollup module with all exports</name>
  <files>src/github/sub-issue-rollup.ts, test/unit/github-sub-issue-rollup.test.ts</files>
  <behavior>
    - buildRollupMarker(42) returns `<!-- forgectl:progress:parent-42 -->`
    - findRollupCommentId: returns comment ID when marker found in body, null when absent, paginates through multiple pages
    - upsertRollupComment: calls createComment when findRollupCommentId returns null, calls updateComment when it returns a number
    - buildSubIssueProgressComment: renders markdown with hidden marker at top, "## Sub-Issue Progress" heading, checklist lines with emojis (completed=check+green, in_progress=hourglass, pending=white square, failed=red X, blocked=no entry), checked boxes for completed only, failed items show error summary, footer "**Progress: N/M complete**"
    - allChildrenTerminal: returns false for empty Map, false when any child not in terminal set, true when all in terminal set
  </behavior>
  <action>
    Create `src/github/sub-issue-rollup.ts` with these exports:

    1. **`RollupOctokitLike` interface** — Minimal Octokit interface with `rest.issues.listComments`, `rest.issues.createComment`, `rest.issues.updateComment`. Follow the same pattern as `OctokitLike` in `comments.ts` but add `listComments` with pagination params (`per_page`, `page`).

    2. **`ChildStatus` interface** — `{ id: string; title: string; url: string; state: "completed" | "in_progress" | "pending" | "failed" | "blocked"; errorSummary?: string }`

    3. **`buildRollupMarker(parentIssueNumber: number): string`** — Returns `<!-- forgectl:progress:parent-{N} -->`

    4. **`findRollupCommentId(octokit, owner, repo, parentIssueNumber): Promise<number | null>`** — Paginate with per_page=100, scan comment.body for marker, return comment.id or null

    5. **`upsertRollupComment(octokit, owner, repo, parentIssueNumber, body): Promise<void>`** — Find existing via findRollupCommentId, update if found, create if not. Self-healing: if user deletes marker comment, next call creates fresh.

    6. **`buildSubIssueProgressComment(parentIssueNumber, children: ChildStatus[]): string`** — Markdown checklist per CONTEXT.md format. Emoji map: completed="check mark", in_progress="hourglass", pending="white square", failed="red X", blocked="no entry sign". Checked `[x]` only for completed. Failed children append ` -- Failed: {errorSummary}`. Footer: `**Progress: {completed}/{total} complete**`. Attribution line at bottom.

    7. **`allChildrenTerminal(childStates: Map<string, string>, terminalStates: Set<string>): boolean`** — Returns false for empty map, iterates values, returns false if any not in terminalStates.

    Create `test/unit/github-sub-issue-rollup.test.ts` with tests for all behaviors listed above. Use mock RollupOctokitLike objects. Write tests FIRST (RED), then implement (GREEN).

    IMPORTANT: Use `em dash` character (--) in failed child display, not the word "dash". Match the CONTEXT.md format exactly for failed children: `Failed: {errorSummary}`.
  </action>
  <verify>
    <automated>FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/github-sub-issue-rollup.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All unit tests pass. Module exports RollupOctokitLike, ChildStatus, buildRollupMarker, findRollupCommentId, upsertRollupComment, buildSubIssueProgressComment, allChildrenTerminal. TypeScript compiles clean (`npx tsc --noEmit`).</done>
</task>

</tasks>

<verification>
```bash
# All new tests pass
FORGECTL_SKIP_DOCKER=true npx vitest run test/unit/github-sub-issue-rollup.test.ts --reporter=verbose

# TypeScript compiles clean
npx tsc --noEmit

# Full suite still passes
FORGECTL_SKIP_DOCKER=true npm test
```
</verification>

<success_criteria>
- src/github/sub-issue-rollup.ts exists with 7 named exports
- All unit tests pass covering marker building, comment search with pagination, upsert (create + update paths), progress comment markdown rendering, all-terminal check edge cases
- TypeScript compiles with no errors (noUnusedLocals enforced)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/28-sub-issue-advanced-features/28-01-SUMMARY.md`
</output>
